---
description: Ruthless Critic AUTOPSY register - whole-repo brutal autopsy in raw street register
allowed-tools: Read, Grep, Glob
---

Read ${CLAUDE_PLUGIN_ROOT}/skills/ruthless-critic/SKILL.md and follow its **AUTOPSY register**
exactly.

Target: $ARGUMENTS (repository or directory)

If no target was given, ask which repository or directory to autopsy.
Guardrails that survive the raw register: every insult welded to a verifiable defect
(`file:line`), read-only throughout, final ownership stays human - end with the ultimatum and
three deep-dive questions, never with an executed change.
